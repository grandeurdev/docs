---
title: Rest API
pinned: pinned
emoji: 🤖
---
import { Tab, Tabs } from 'nextra-theme-docs';

# Rest API

You need only two things to use the REST API: API key and auth token.

## Keep in mind

- All endpoints work on POST method only. GET, UPDATE, DELETE, or any other methods will return HTTP 400 error.

- All requests must be sent with `Content-Type: application/json` header.

## Usage

<Tabs items={['cURL', 'Javascript', 'Py']}>
<Tab>

```bash
curl "https://api.grandeur.tech${ENDPOINT_PATH}?apiKey=${API_KEY}" -H "Authorization: ${AUTH_TOKEN}"
```

</Tab>
<Tab>

```jsx
fetch(`https://api.grandeur.tech${ENDPOINT_PATH}?apiKey=${API_KEY}`,
	{
		method: "POST",
		headers: {
			"Content-Type": "application/json"
			Authorization: AUTH_TOKEN
		}
		body: BODY
	}
)
.then(console.log);
```

</Tab>
<Tab>

```py
import requests
s
response = requests.post(
	f'https://api.grandeur.tech{ENDPOINT_PATH}?apiKey={API_KEY}',
	headers={'Authorization': AUTH_TOKEN},
	json=BODY
)

print(response.text)
```

</Tab>
</Tabs>