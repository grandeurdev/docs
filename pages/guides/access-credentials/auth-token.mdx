---
title: Auth Token
---
import ReactPlayer from 'react-player';

# Auth Token

You can generate multiple auth tokens. Auth tokens lead to sandboxed access in custom apps so that multiple users of a particular custom app get different level of privilege of use. 

Here is how you can generate an Auth token for your app:

<ReactPlayer playing controls volume={0} width={832} height={468} url="/demos/guides/generate-auth-token.webm"/>