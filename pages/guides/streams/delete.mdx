---
title: Delete a variable's stream
---
import ReactPlayer from "react-player";

# Delete a variable's stream

When you delete a variable, its stream is automatically deleted.

<ReactPlayer playing controls volume={0} width={832} height={468} url="/demos/guides/console-delete-device-variable.webm" />